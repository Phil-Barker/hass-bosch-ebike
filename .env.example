# Home Assistant Deployment Configuration
# Copy this file to .env and update with your Home Assistant details
# Then source it before running deploy-dev.sh:
#   source .env && ./deploy-dev.sh

# Home Assistant hostname or IP address
export HA_HOST=homeassistant.local
# export HA_HOST=192.168.1.100

# SSH username (usually 'root' for HA SSH add-on)
export HA_USER=root

# SSH port (default is 22, but check your SSH add-on config)
export HA_PORT=22

# Home Assistant config directory (usually /config for HA OS)
export HA_CONFIG_DIR=/config

